# AWS S3 Configuration
aws.region=us-east-1
aws.s3.bucket-name=dropbox-secure-storage-dev
aws.s3.enabled=false

# Application Configuration
spring.application.name=dropbox-application

# Storage Configuration
storage.type=memory
# Possible values: memory, s3

# Security Configuration
security.s3.verify-on-startup=true
